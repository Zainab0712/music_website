// const test = require('node:test');
// const assert = require('node:assert/strict');
// const { normalizeVolume } = require('../volume');

// test('maps the range slider from 0-100 to a 0-1 audio volume', () => {
//   assert.equal(normalizeVolume(0), 0);
//   assert.equal(normalizeVolume(50), 0.5);
//   assert.equal(normalizeVolume(100), 1);
// });

// test('clamps out-of-range values', () => {
//   assert.equal(normalizeVolume(-10), 0);
//   assert.equal(normalizeVolume(120), 1);
// });
